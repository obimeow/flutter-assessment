import 'package:flutter/material.dart';

class SavingsAccount3 extends StatelessWidget {
  const SavingsAccount3({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text('Savings Account 3')),
      ),
    );
  }
}
