import 'package:flutter/material.dart';

class SavingsAccount4 extends StatelessWidget {
  const SavingsAccount4({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text('Savings Account 4')),
      ),
    );
  }
}
