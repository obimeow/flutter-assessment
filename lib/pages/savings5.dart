import 'package:flutter/material.dart';

class SavingsAccount5 extends StatelessWidget {
  const SavingsAccount5({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text('Savings Account 5')),
      ),
    );
  }
}
