import 'package:flutter/material.dart';

class SavingsAccount1 extends StatelessWidget {
  const SavingsAccount1({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text('Savings Account 1')),
      ),
    );
  }
}
