import 'package:flutter/material.dart';

class SavingsAccount2 extends StatelessWidget {
  const SavingsAccount2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text('Savings Account 2')),
      ),
    );
  }
}
